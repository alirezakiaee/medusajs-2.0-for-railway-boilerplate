"use client"

import { GalleryConfigurator } from "./GalleryConfigurator"

// Zustand is global — no provider wrapper needed.
export function GalleryConfiguratorPage() {
  return <GalleryConfigurator />
}
